<Window x:Class="UP7._2.TableEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UP7._2"
        Title="Редактирование таблицы кодов" 
        Height="350" 
        Width="450"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="CanMinimize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="CodesDataGrid" AutoGenerateColumns="True" CanUserAddRows="True" CanUserDeleteRows="True" SelectionMode="Single" HeadersVisibility="Column"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="80" Click="SaveButton_Click" Margin="0,0,10,0" ToolTip="Сохранить изменения и закрыть окно"/>
            <Button Content="Отмена" Width="80" Click="CancelButton_Click" ToolTip="Закрыть без сохранения"/>
        </StackPanel>
    </Grid>
</Window>



